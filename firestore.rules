rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Placeholder rules — full implementation in issue #8
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
