import { registerGlobals } from '@livekit/react-native'

// registerGlobals() must be called once before any LiveKit usage.
// Called at module level here — import this file early in the app lifecycle.
registerGlobals()
